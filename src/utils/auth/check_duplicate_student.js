// import User from '../models/lecturer_model.js';
// import { BadRequestError } from '../errors/index.js';

// export const checkStudentDuplicate = async (matricNumber) => {
//   const existing = await User.findOne({ matricNumber }).lean();
//   if (existing) {
//     throw new BadRequestError('Matric number already in use');
//   }
// };
